This application runs an autonomous file watcher (assets/build). It is managed by the host: do NOT start, stop or restart it yourself unless the user explicitly asks you to.
{% if app.env == 'local' %}
Local wiring: the Symfony application is already wired to its local dependencies, or the user wires it themselves by running `wex setup::install --env local`. Never wire it manually (no editing of composer paths, symlinks or autoload config to point at local sources) — run that command, or ask the user to.
{% endif %}
