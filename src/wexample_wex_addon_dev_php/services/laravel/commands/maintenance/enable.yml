description: Enable Laravel maintenance mode (php artisan down)
decorators:
  - name: tags
    args:
      - domain:app-lifecycle
      - domain:framework
      - domain:language-php
      - effect:write
      - audience:agent-safe
      - scope:app
      - scope:local
  - name: attach
    args:
      position: after
      command: app::maintenance/enable
scripts:
  - runner: docker
    service: laravel
    shell: sh
    script: php artisan down
    app_should_run: true
