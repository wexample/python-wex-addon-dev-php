services:
  phpmyadmin:
    extends:
      file: ${SERVICE_PHPMYADMIN_COMPOSE}
      service: phpmyadmin
    environment:
      - PMA_USER=${SERVICE_MYSQL_USER}
      - PMA_PASSWORD=${SERVICE_MYSQL_PASSWORD}
